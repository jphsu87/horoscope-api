services:
  - type: web
    name: horoscope-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: DATA_DIR
        value: ./data
      - key: PYTHON_VERSION
        value: 3.11.9